import 'package:flutter/material.dart';

class PerformacePage extends StatelessWidget {
  const PerformacePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Performance Page'));
  }
}
