import 'package:flutter/material.dart';

class GainersPage extends StatelessWidget {
  const GainersPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Gainers Page"));
  }
}
