import 'package:flutter/material.dart';

class BuyStopPage extends StatelessWidget {
  const BuyStopPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Stop buy page"));
  }
}
