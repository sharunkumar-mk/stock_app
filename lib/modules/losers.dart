import 'package:flutter/material.dart';

class LosersPage extends StatelessWidget {
  const LosersPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Losers Page"));
  }
}
