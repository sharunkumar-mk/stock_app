import 'package:flutter/material.dart';

class BuyLimitPage extends StatelessWidget {
  const BuyLimitPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Buy limit page"));
  }
}
